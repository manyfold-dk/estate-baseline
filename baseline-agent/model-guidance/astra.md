# GPT-6 Astra -- optional guide

Select only for a confirmed GPT-6 Astra session. Neutral policy remains the default. If a
model switch cannot retract this guidance, start a fresh specialized session or stay neutral.

State the authorized completion criteria explicitly. Continue through routine implementation
decisions while preserving material clarification and human review gates. Keep tests scoped
to changed behavior plus mandatory repository checks. Use bounded delegation only when
available, authorized and useful. Preserve the effective effort for comparison; this estate's
initial comparison uses `high`, not an asserted optimum.

Source checked 2026-09-05: [official GPT-6 Astra prompting guidance](https://developers.openai.com/api/docs/guides/latest-model). These are local
workflow adaptations, not additional authorization or a claim of measured efficiency.
