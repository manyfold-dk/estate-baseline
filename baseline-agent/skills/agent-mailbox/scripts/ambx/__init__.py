"""agent-mailbox: filesystem coordination substrate for multi-agent work.

Package named ``ambx`` (not ``mailbox``) so it never shadows the Python stdlib
``mailbox`` module when ``scripts/`` is on ``sys.path``.
"""
